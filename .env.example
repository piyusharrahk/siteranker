# Netlify Functions -> Supabase
SUPABASE_URL=
# Prefer service role for full CRUD in functions; otherwise use anon and set Row Level Security correctly
SUPABASE_SERVICE_ROLE=
# If you choose to use anon key instead, uncomment
# SUPABASE_ANON_KEY=

# Optional: client override (leave blank to use same origin)
# VITE_API_BASE=